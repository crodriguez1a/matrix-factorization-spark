name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
    steps:
    - name: Python Pytest Github Action
      uses: fylein/python-pytest-github-action@v2
      with:
        args: pip3 install -r requirements.txt && pytest test/
